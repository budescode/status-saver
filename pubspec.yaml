name: whatsappsave
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  path_provider: ^1.6.27
  permission_handler: ^5.0.1+1
  video_player: ^2.0.0
  chewie: ^1.0.0
  line_icons: any
  google_fonts: any
  share: ^2.0.1
  fluttertoast: ^8.0.6
  flutter_share_me: ^0.10.0
  wakelock: ^0.4.0 # keeping screen on
  video_thumbnail: ^0.3.3
  image_picker: ^0.6.2+3
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - embed/